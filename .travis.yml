language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t kevinlien/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true kevinlien/docker-react npm run test